using System;
using UnityEngine;
public class ResourceSpawn : MonoBehaviour
{
    // [Header("SpawnSettings")]
    // public gameObject diamond;
    // public float spawnChance;
    // public int maxDiamondsToSpawn = 5;

    // [Header("Raycast settings")] // if needed


    void Start()
    {
        // spawnDiamonds();
    }

    void Update()
    {
        // add if/else statement here to handle spawn timing might be better to use Fixed Update class
    }

    public void spawnDiamonds()
    {
        // if (diamondSpawned >= maxDiamondsToSpawn)
        // {
        //    return; // spawn no more than 5 diamonds at a time
        // }
    }
}
